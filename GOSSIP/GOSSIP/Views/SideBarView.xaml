<UserControl x:Class="GOSSIP.Views.SideBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GOSSIP.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="476">
    <Grid>
        <Border Background="{DynamicResource PrussianBlue}">
            <StackPanel>
                <Button Style="{DynamicResource SideBarButton}" Command="{Binding ShowPostsListCommand}" Content="Topics" Margin="0 40 0 0"/>
                <Button Style="{DynamicResource SideBarButton}" Content="Tags"/>
                <Button Style="{DynamicResource SideBarButton}" Command="{Binding ShowChatsCommand}" Content="Messages"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
